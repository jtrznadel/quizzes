import 'package:flutter/material.dart';

class QuizzConfigureScreen extends StatelessWidget {
  const QuizzConfigureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Quizz configure'),
    );
  }
}
