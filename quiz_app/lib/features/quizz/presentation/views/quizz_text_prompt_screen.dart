import 'package:flutter/material.dart';

class QuizzTextPromptScreen extends StatelessWidget {
  const QuizzTextPromptScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Text prompt'),
    );
  }
}
